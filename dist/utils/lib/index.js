"use strict";

var suffixList = require('./suffixList');

var suffixes = require('./suffixes');

var directionals = require('./directionals');

var directionalList = require('./directionalList');

module.exports = {
  suffixList: suffixList,
  suffixes: suffixes,
  directionals: directionals,
  directionalList: directionalList
};